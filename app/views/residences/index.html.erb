<%= render 'listaResidencia', residencias: @residences %>
<!--<p id="notice"><%= notice %></p>

<h1>Residences</h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Provincia</th>
      <th>Partido</th>
      <th>Ciudad</th>
      <th>Direccion</th>
      <th>Img link</th>
      <th>Desc</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @residences.each do |residence| %>
      <tr>
        <td><%= residence.nombre %></td>
        <td><%= residence.provincia %></td>
        <td><%= residence.partido %></td>
        <td><%= residence.ciudad %></td>
        <td><%= residence.direccion %></td>
        <td><%= residence.img_link %></td>
        <td><%= residence.desc %></td>
        <td><%= link_to 'Show', residence %></td>
        <td><%= link_to 'Edit', edit_residence_path(residence) %></td>
        <td><%= link_to 'Destroy', residence, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Residence', new_residence_path %>
-->
